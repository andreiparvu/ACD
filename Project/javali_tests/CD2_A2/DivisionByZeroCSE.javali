class Main {
    int f() {
        write(999);
        return 20;
    }
    
    int endless() {
        while (true) { }
        return 0;
    }

    void main() {
        int zero;
        boolean b;
        
        zero = read();
        
        // b = (endless() > 0) || ((10 / zero) == 0);
        write(f() + (10 / zero - 10 / zero)); writeln();


        write(1);
    }
}
