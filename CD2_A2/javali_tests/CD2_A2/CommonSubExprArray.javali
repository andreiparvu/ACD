class Main {
  void main() {
    int a, b, c, d, e, f, g, h, k;
    
    int[] array;
    
    array = new int[100];

    a = read();
    b = read();
    c = read();

    e = array[a * 2 + c];
    f = array[a * 2 + c + 1];
    h = array[a * 2 + (1 + c)];
    k = array[a * 2 + (c + 1)];

    write(e);
    write(f);
  }
}

