class Main {
  void main() {
    int a, b, c, d, e, x, y, f, g;

    b = read();
    c = read();
    e = read();
    a = b + c;
    x = b * c + e;
    f = (b + c) + (b + c);

    if (a <= b) {
      write(b + c);
      write(a * x);
    } else {
      write(a * x);
    }

    write(f);
    write(a * x);
  }
}

