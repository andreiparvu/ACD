class Main {
  int doit(int x, int y) {
    int a, b;
    int z;

    a = x + 10;
    b = y + 20;

    if (x <= 20) {
      z = x + y;
    } else {
      z = x + y + 20;
    }

    return x + y;
  }
  void main() {
    doit(10, 20);
  }
}

